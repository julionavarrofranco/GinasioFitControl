<Window x:Class="FitControlAdmin.MemberEvaluationHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Histórico de Avaliações Físicas - {Binding MemberName}"
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#313443"
        ResizeMode="NoResize">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   Grid.Row="0"
                   Text="Histórico de Avaliações Físicas"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="#e6e6e8"
                   Margin="0,0,0,25"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock x:Name="MemberInfoText"
                       Foreground="#95a5a6"
                       FontSize="14"
                       Margin="0,0,0,10"/>
        </StackPanel>

        <Border Grid.Row="2"
                Background="#272936"
                CornerRadius="10"
                Padding="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <DataGrid x:Name="EvaluationsDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          GridLinesVisibility="All"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Data Avaliação" Binding="{Binding DataAvaliacao, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                        <DataGridTextColumn Header="Peso (kg)" Binding="{Binding Peso, StringFormat=F2}" Width="100"/>
                        <DataGridTextColumn Header="Altura (m)" Binding="{Binding Altura, StringFormat=F2}" Width="100"/>
                        <DataGridTextColumn Header="IMC" Binding="{Binding Imc, StringFormat=F2}" Width="80"/>
                        <DataGridTextColumn Header="Massa Muscular (kg)" Binding="{Binding MassaMuscular, StringFormat=F2}" Width="140"/>
                        <DataGridTextColumn Header="Massa Gorda (kg)" Binding="{Binding MassaGorda, StringFormat=F2}" Width="130"/>
                        <DataGridTextColumn Header="Observações" Binding="{Binding Observacoes}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,25,0,0">
            <Button Content="Fechar"
                    Click="CloseButton_Click"
                    Padding="20,10"
                    Width="120"
                    Background="#34495E"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    FontSize="14">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#34495E"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2c3e50"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
