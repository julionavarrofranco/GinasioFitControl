<Window x:Class="FitControlAdmin.CreateEditTrainingPlanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Criar Plano de Treino" Height="580" Width="750"
        WindowStartupLocation="CenterOwner"
        Background="#313443"
        Foreground="#e6e6e8"
        FontFamily="Segoe UI"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#95a5a6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1f2937"/>
            <Setter Property="Foreground" Value="#e6e6e8"/>
            <Setter Property="BorderBrush" Value="#34495E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
        </Style>
        <Style TargetType="Button" x:Key="BtnPrimary">
            <Setter Property="Background" Value="#02acc3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="Button" x:Key="BtnSecondary">
            <Setter Property="Background" Value="#34495E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="Button" x:Key="BtnDanger">
            <Setter Property="Background" Value="#e74c3c"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Nome do plano:"/>
            <TextBox x:Name="NomeTextBox"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Observações:"/>
            <TextBox x:Name="ObservacoesTextBox"
                     Height="60"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalContentAlignment="Top"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Exercícios do plano:"
                       FontSize="14"
                       FontWeight="Bold"
                       Foreground="#e6e6e8"
                       VerticalAlignment="Center"/>
            <Button Grid.Column="1"
                    Content="+ Adicionar exercício"
                    Style="{StaticResource BtnPrimary}"
                    Click="AddExerciseButton_Click"/>
        </Grid>

        <DataGrid Grid.Row="6"
                  x:Name="ExercisesDataGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Background="#1f2937"
                  Foreground="Black"
                  RowBackground="#1f2937"
                  AlternatingRowBackground="#252f3f"
                  BorderThickness="1"
                  BorderBrush="#34495E"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  MinHeight="150">
            <DataGrid.Resources>
                 <Style TargetType="DataGridCell">
                    <Setter Property="Foreground" Value="#FFE6E6E8"/>
                </Style>

                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#ff1a4d5c"/> 
                    <Setter Property="Foreground" Value="#e6e6e8"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ordem" Binding="{Binding Ordem}" Width="50"/>
                <DataGridTextColumn Header="Exercício" Binding="{Binding NomeExercicio}" Width="*"/>
                <DataGridTextColumn Header="Séries" Binding="{Binding Series}" Width="60"/>
                <DataGridTextColumn Header="Reps" Binding="{Binding Repeticoes}" Width="50"/>
                <DataGridTextColumn Header="Carga" Binding="{Binding Carga}" Width="70"/>
                <DataGridTemplateColumn Header="" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Editar"
                                        Style="{StaticResource BtnSecondary}"
                                        Click="EditExerciseButton_Click"
                                        Tag="{Binding}"
                                        Padding="8,4" Margin="0,0,4,0" FontSize="12"/>
                                <Button Content="Remover"
                                        Style="{StaticResource BtnDanger}"
                                        Click="RemoveExerciseButton_Click"
                                        Tag="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Style="{StaticResource BtnSecondary}" Click="CancelButton_Click" Margin="0,0,10,0"/>
            <Button Content="Guardar" Style="{StaticResource BtnPrimary}" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
